### Documentation Assets

This repository includes a `docs/` directory that contains all supporting materials used throughout the project. These assets include:

- **Images** used in the report, dashboard walkthrough, and system diagrams  
- **videos** showcasing the workflow, model outputs, and ICU forecasting dashboard
- **vThe final written report** located in docs/, which provides a comprehensive explanation of the project’s background, methodology, models, results, and system design. it serves as the primary reference document for understanding the full end-to-end workflow of the project.

The `docs/` folder is referenced throughout the repository so that notebooks, the dashboard, and the final report can load media files without broken paths.  
If you clone or fork this repository, please keep the `docs/` directory in its original location to ensure all embedded media continues to display correctly.
